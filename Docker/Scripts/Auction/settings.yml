apiVersion: apps/v1
kind: Deployment
metadata:
  name: auction-settings
spec:
  selector:
    matchLabels:
      app: auction-settings
  template:
    metadata:
      labels:
        app: auction-settings
    spec:
      containers:
      - name: auction-settings
        image: kozlovas/auction-settings
        imagePullPolicy: IfNotPresent
        startupProbe:
          httpGet:
            path: /checkvault
            port: 80
            scheme: HTTP
            host: 10.108.29.192
          failureThreshold: 60
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: "1"
          requests:
            cpu: "0.5"
---
apiVersion: v1
kind: Service
metadata:
  name: auction-settings-clusterip
spec:
  type: ClusterIP
  selector:
    app: auction-settings
  ports:
  - port: 80
    targetPort: 80
