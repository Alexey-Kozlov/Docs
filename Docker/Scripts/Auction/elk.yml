apiVersion: apps/v1
kind: Deployment
metadata:
  name: auction-elk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auction-elk
  template:
    metadata:
      labels:
        app: auction-elk
    spec:
      containers:
      - name: auction-elk
        image: kozlovas/auction-elk
        imagePullPolicy: IfNotPresent
        startupProbe:
          httpGet:
            path: /checkvault
            port: 80
            scheme: HTTP
            host: 10.108.29.192
          failureThreshold: 60
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: "1"
          requests:
            cpu: "0.5"
---
apiVersion: v1
kind: Service
metadata:
  name: auction-elk-clusterip
spec:
  type: ClusterIP
  selector:
    app: auction-elk
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: auction-elk-clusterip-grpc
spec:
  type: ClusterIP
  selector:
    app: auction-elk
  ports:
  - port: 7784
    targetPort: 7784
    name: grpc
