apiVersion: apps/v1
kind: Deployment
metadata:
  name: auction-bid
spec:
  selector:
    matchLabels:
      app: auction-bid
  template:
    metadata:
      labels:
        app: auction-bid
    spec:
      containers:
      - name: auction-bid
        image: kozlovas/auction-bidding
        imagePullPolicy: IfNotPresent
        startupProbe:
          httpGet:
            path: /checkvault
            port: 80
            scheme: HTTP
            host: 10.108.29.192
          failureThreshold: 60
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: "1"
          requests:
            cpu: "0.5"
---
apiVersion: v1
kind: Service
metadata:
  name: auction-bid-clusterip
spec:
  type: ClusterIP
  selector:
    app: auction-bid
  ports:
  - port: 80
    targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: auction-bid-clusterip-grpc
spec:
  type: ClusterIP
  selector:
    app: auction-bid
  ports:
  - port: 7780
    targetPort: 7780
    name: grpc
